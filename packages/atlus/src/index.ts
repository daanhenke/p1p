// @p1p/atlus — the PSX sector-table archive format (shared across Atlus PSX titles).
// Everything else (text codec, script VM, all datatypes) is Persona 1–specific and lives in the
// `persona1` package.
export * from "./archive.js";
